#include <Distributors/distributor.hpp>
#include <Utilities/utils.hpp>

#include <iostream>

using namespace std;

tuple<int, int> Distributors::Bucket(vector<int> &vals, bool auxInfo)
{
	int runs = vals.size() - 1, cmps = 0, swps = 0;
	return { cmps, swps };
}
