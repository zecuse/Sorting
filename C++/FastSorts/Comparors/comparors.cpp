#include <Comparors/comparors.hpp>
#include <Utilities/utils.hpp>

#include <iostream>

using namespace std;

tuple<int, int> Comparors::Merge(vector<int> &vals, bool auxInfo)
{
	int runs = vals.size() - 1, cmps = 0, swps = 0;
	return { cmps, swps };
}
